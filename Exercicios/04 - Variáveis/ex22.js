//VARIÁVEIS COMPOSTAS

var num = [1, 5, 3]; //Ordem dentro dos colchetes: 0 - 1 - 2

// Repetição "for" padrão para mostrar um console.log em cada elemento da variável automaticamente
for (var res = 0; res < num.length; res++) {
  console.log(`Posição ${res}, elemento ${num[res]}`);
}
